# Application
PROJECT_NAME="HRMS Lite API"
VERSION="1.0.0"
LOG_LEVEL=INFO

# Database - Supabase PostgreSQL (recommended for production)
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:your-password-here@db.xxxxx.supabase.co:5432/postgres

# For local development (SQLite fallback if DATABASE_URL not set)
# DATABASE_URL=sqlite:///./hrms.db

# CORS - comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
